package Chapter1.Section1;

/**
 * Ex. 1.1.34 <br>
 * 15.01.2022
 * @author xairaven
 */
public class Task_34 {
    public static void main(String[] args) {
        System.out.println("-- Exercise 1.1.34 --");
        System.out.println("""
                Print the maximum and minimum numbers:
                   - Filter: two ints (max and min), two if-else constructions.
                    
                Print the median of the numbers:
                   - Requires saving all values in array
                
                Print the k th smallest value, for k less than 100:
                   - Filter: Could be implemented with an array of size K
                
                Print the sum of the squares of the numbers:
                   - Filter, sum += Math.pow(every-number, 2)
                
                Print the average of the N numbers:
                   - Filter, sum += every-number, average = sum / N
                
                Print the percentage of numbers greater than the average:
                   - Requires saving all values in array
                
                Print the N numbers in increasing order:
                   - Requires saving all values in array
                
                Print the N numbers in random order:
                   - Requires saving all values in array
                """);
    }
}
